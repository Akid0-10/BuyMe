<?php

// Assign the value from database server
$server = "localhost";
$username = "root";
$password = "";
$dbName = "buyme";

// Creaate connection to database
$conn = mysqli_connect($server, $username, $password, $dbName);

?>